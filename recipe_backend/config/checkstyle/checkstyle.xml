<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
 Minimal Checkstyle configuration.

 Purpose:
 - Ensure Gradle's `checkstyleMain` task exists and can run in CI environments that
   expect it, without introducing additional style enforcement on this codebase.
 - This configuration intentionally contains no checks beyond the required TreeWalker.

 Note:
 - If stricter style enforcement is desired later, add modules under TreeWalker.
-->
<module name="Checker">
    <module name="TreeWalker"/>
</module>
