spring.application.name=recipebackend

# Server
server.port=${PORT:3001}

# MongoDB (use env vars provided by the platform)
spring.data.mongodb.uri=${MONGODB_URL:mongodb://localhost:27017}
spring.data.mongodb.database=${MONGODB_DB:myapp}

# Actuator
management.endpoints.web.exposure.include=health,info,metrics

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha

# Respect X-Forwarded-* / Forwarded headers so redirects preserve external scheme/host/port
server.forward-headers-strategy=framework
