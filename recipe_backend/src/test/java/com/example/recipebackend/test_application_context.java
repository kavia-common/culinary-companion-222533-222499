package com.example.recipebackend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic smoke test to ensure the Spring application context starts successfully.
 */
@SpringBootTest
public class test_application_context {

	@Test
	void contextLoads() {
		// If the context fails to start, this test will fail.
	}
}
